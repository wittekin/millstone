The work is approved. Commit it now.

1. Stage all changes with `git add -A`.
2. Write a clear commit message that matches the actual change.
3. Commit immediately. Do not make more edits.

Use:
- a short summary line
- an optional body when it adds value
- the footer `Generated with millstone orchestrator`
