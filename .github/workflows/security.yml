name: Security

on:
  pull_request:
  schedule:
    - cron: "0 9 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pip-audit:
    name: Dependency vulnerability audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .[security]

      - name: Run pip check
        run: pip check

      - name: Run pip-audit
        run: |
          # Pygments is pulled in via pip-audit -> rich and is not part of the shipped
          # millstone runtime dependency set. pip-audit currently reports CVE-2026-4539
          # with no fixed version available, so ignore it until upstream publishes a fix.
          pip-audit --ignore-vuln CVE-2026-4539
